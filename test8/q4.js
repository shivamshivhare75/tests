// Find the index position of b in this array var arr= ['a','b','c','d'];

var arr = ['a', 'b', 'c', 'd'];
for(let i in arr){
    if(arr[i]==='b'){
        console.log(`index position of b is  ${i}`);
    }
}